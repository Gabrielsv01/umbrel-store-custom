# Local model weights, Hugging Face cache and generated audio (multi-GB /
# ephemeral, re-created automatically) — never commit these. Saved reference
# voices (data/data/voices) are the one thing under here worth versioning, so
# they're explicitly re-included below.
data/*
!data/data
data/data/*
!data/data/voices
