version: '3.8'

services:
  webhook-gateway:
    build:
      context: .
      dockerfile: code/Dockerfile
    container_name: webhook-gateway
    restart: unless-stopped
    ports:
      - "5124:5124"
    environment:
      - PORT=5124
