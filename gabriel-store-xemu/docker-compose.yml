services:
  xemu:
    image: linuxserver/xemu:0.8.118
    container_name: xemu
    security_opt:
      - seccomp:unconfined
    privileged: true
    environment:
      - PUID=1000
      - PGID=995
      - TZ=America/Sao_Paulo
      - DISPLAY_WIDTH=1280
      - DISPLAY_HEIGHT=720
    volumes:
      - ${APP_DATA_DIR}/data/config:/config
      - ${APP_DATA_DIR}/data/games:/games
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - 5141:3001
    devices:
      - /dev/dri:/dev/dri
      - /dev/input:/dev/input
    shm_size: "10gb"
    restart: unless-stopped