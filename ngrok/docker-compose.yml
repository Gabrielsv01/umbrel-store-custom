services:
  ngrok:
    image: ngrok/ngrok:latest
    command:
      - http
      - "${LOCAL_PORT}"
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    ports:
      - "4040:4040"  # Painel web do ngrok
    restart: on-failure
